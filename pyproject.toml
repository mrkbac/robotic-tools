[project]
name = "robotic-tools"
version = "0.1.0"
requires-python = ">=3.10"

[tool.uv.sources]

[tool.uv.workspace]
members = [
    "digitalis",
    "mcap-ros2-support-fast",
    "pymcap-cli",
    "small-mcap",
    "websocket-proxy",
    "websocket-bridge",
]
